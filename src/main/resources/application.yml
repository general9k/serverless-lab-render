spring:
  application:
    name: lab_4
  liquibase:
    change-log: changelog/master.xml
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true
    show-sql: true
server:
  port: 5000